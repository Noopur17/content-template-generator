import ProductForm from "../../components/ProductForm";

export default function RetailGeneratorPage() {
  return (
    <main style={{ maxWidth: "900px", margin: "0 auto", padding: "24px" }}>
      <ProductForm />
    </main>
  );
}